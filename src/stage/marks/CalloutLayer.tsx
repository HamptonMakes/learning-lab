/**
 * CalloutLayer — HTML overlay for callout bubbles and unchanged pills anchored to paths.
 * CONTRACT (owned by the layers task). Placeholder until the layers land.
 */
export function CalloutLayer() {
  return null
}
