/**
 * MarkLayer — SVG overlay for cross-node marks: conflict bolts, compare links + verdict chips,
 * flow arrows. CONTRACT (owned by the layers task). Placeholder until the layers land.
 */
export function MarkLayer() {
  return null
}
