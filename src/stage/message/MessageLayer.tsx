/**
 * MessageLayer — draws message tokens (flying on arcs, parked in trays, transient flights, decks).
 * CONTRACT (owned by the layers task): reads the frame via useStageFrame() and rects via useGeometry().
 * Placeholder until the layers land.
 */
export function MessageLayer() {
  return null
}
