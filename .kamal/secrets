# Secrets come from Proton Pass CLI at deploy time. This file is safe for git: it contains
# commands, never values. Requires `pass-cli` (brew) logged in, vault "kamal".
KAMAL_REGISTRY_PASSWORD=$(pass-cli item view --vault-name kamal --item-title DockerKey --field note)
